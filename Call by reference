#include<stdio.h>

void swap(int*,int*);
int main() 
{ 
  int a=40;
  int b=30;
 

  printf("before swapping the value   main a=%d,b%d\n",a,b);
 (*num)+=100; 
  
printf("after after swapping  the value in main a=%d,b=%d\n",a,b);

} 
 void swap(int*a, int*b)
{
 int temp;
temp=*a;
*a=*b;
 *b=temp;
  printf("after swapping the value in function a=%d,b=%d\n",*a,*b);
return 0;
}
