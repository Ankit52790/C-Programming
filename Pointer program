#include<stdio.h>

int main()
{
 printf("Let's lern about pointer\n:");
 int a=76;
 int *ptra=&a;
 printf("The address of pointer to a is %p\n",&*ptra);
 printf("The value of a is %d\n",*ptra);
 printf("The value of a is%d\n",a);
 return 0;
}
